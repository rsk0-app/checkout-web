apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-checkout-web
  labels:
    app: checkout-web
spec:
  type: ClusterIP
  selector:
    app: checkout-web
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
