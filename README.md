# checkout-web

A minimal Next.js (App Router) storefront checkout flow.

## Develop

```bash
npm install
npm run dev   # http://localhost:3000
```

Pages live under `app/`. Container build via the root `Dockerfile`.
